<!DOCTYPE html>
<html>
<head>
    <style>
        /* Standard Business Card Size: 3.5in x 2in (approx 336px x 192px at 96dpi, but PDF scaling handles standard units better) 
           We'll use a wrapper that fits a card aspect ratio. */
        body { margin: 0; padding: 0; font-family: 'Arial', sans-serif; background-color: #fff; }
        .card {
            width: 3.5in;
            height: 2in;
            position: relative;
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            color: white;
            padding: 20px;
            box-sizing: border-box;
            display: flex;
            align-items: center;
            border-radius: 10px; /* Optional rounded corners for preview, though print usually cuts square */
            overflow: hidden;
        }
        .content {
            z-index: 2;
            flex: 1;
        }
        .name { font-size: 20px; font-weight: bold; margin: 0; text-transform: uppercase; letter-spacing: 1px; }
        .title { font-size: 10px; margin-bottom: 15px; opacity: 0.9; }
        .details { font-size: 9px; line-height: 1.4; opacity: 0.9; }
        .qr-container {
            width: 80px;
            height: 80px;
            background: white;
            padding: 5px;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-left: 20px;
        }
        .qr-container img { width: 100%; height: 100%; }
    </style>
</head>
<body>
    <div class="card">
        <div class="content">
            <h1 class="name">{{name}}</h1>
            <div class="title">{{title}}</div>
            <div class="details">
                <div>{{phone}}</div>
                <div>{{email}}</div>
                <div>{{website}}</div>
            </div>
        </div>
        <div class="qr-container">
            <!-- generating a meCard/vCard string for the QR code -->
            <img src="{{{qrcode (concat 'MECARD:N:' name ';TEL:' phone ';EMAIL:' email ';URL:' website ';;') 'text' 3}}}" alt="Contact QR">
        </div>
    </div>
</body>
</html>
