<!DOCTYPE html>
<html>
<head>
    <style>
        /* Standard 4x6 inch label */
        body { font-family: 'Arial', sans-serif; margin: 0; padding: 15px; background: white; width: 4in; height: 6in; box-sizing: border-box; border: 1px solid #ddd; }
        .header { border-bottom: 2px solid black; padding-bottom: 10px; margin-bottom: 10px; }
        .from { font-size: 10px; text-transform: uppercase; margin-bottom: 10px; }
        .to { font-size: 14px; margin-bottom: 20px; }
        .to b { font-size: 18px; display: block; margin-bottom: 5px; }
        .ship-meta { display: flex; justify-content: space-between; border-top: 2px solid black; border-bottom: 2px solid black; padding: 10px 0; margin-bottom: 20px; }
        .meta-item { text-align: center; }
        .meta-label { font-size: 10px; text-transform: uppercase; }
        .meta-value { font-size: 16px; font-weight: bold; }
        .tracking-barcode { text-align: center; margin-bottom: 20px; }
        .tracking-barcode img { width: 90%; height: 80px; }
        .tracking-number { text-align: center; font-family: monospace; font-size: 14px; margin-top: 5px; }
        .footer { font-size: 10px; text-align: center; margin-top: auto; }
        .service-type { font-size: 48px; font-weight: bold; text-align: center; border: 4px solid black; padding: 10px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="header">
        <div class="from">
            From:<br>
            <strong>{{sender.name}}</strong><br>
            {{sender.address}}<br>
            {{sender.city}}, {{sender.state}} {{sender.zip}}
        </div>
    </div>

    <div class="to">
        To:<br>
        <b>{{recipient.name}}</b>
        <div>{{recipient.address}}</div>
        <div>{{recipient.city}}, {{recipient.state}} {{recipient.zip}}</div>
    </div>

    <div class="ship-meta">
        <div class="meta-item">
            <div class="meta-label">Weight</div>
            <div class="meta-value">{{package.weight}}</div>
        </div>
        <div class="meta-item">
            <div class="meta-label">Ship Date</div>
            <div class="meta-value">{{package.shipDate}}</div>
        </div>
    </div>

    <div class="service-type">
        {{package.service}}
    </div>

    <div class="tracking-barcode">
        <img src="{{{barcode package.trackingNumber 'CODE128' 400 100 false}}}" alt="Tracking Barcode">
        <div class="tracking-number">{{package.trackingNumber}}</div>
    </div>
    
    <div class="footer">
        Order Ref: {{package.reference}}
    </div>
</body>
</html>
